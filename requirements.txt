# 时序数据管道优化方案依赖包

# 核心依赖
numpy>=1.21.0
torch>=1.10.0

# 方案1: 共享内存（Python 3.8+内置，无需额外安装）

# 方案2: Arrow/Parquet
pyarrow>=10.0.0
pandas>=1.3.0

# 方案3: HDF5混合策略
h5py>=3.7.0

# Spark相关（如果从HDFS读取）
pyspark>=3.2.0  # 可选，用于HDFS数据读取

# 可视化和测试
matplotlib>=3.5.0

# 数据处理工具
tqdm>=4.62.0  # 进度条
