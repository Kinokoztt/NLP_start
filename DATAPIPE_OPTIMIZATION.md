# 时序神经网络 DataPipe 优化方案

## 当前方案分析

### 现有流程
```
HDFS -> Spark -> Local File -> np.memmap -> Torch Dataset
```

### 主要问题
1. **内存映射速度瓶颈**：memmap虽然避免了全量加载，但读取速度远低于真实内存（约10-50倍差距）
2. **双倍存储开销**：原始文件 + memmap文件
3. **准备时间过长**：向memmap写入数据极慢，训练前等待时间长

## 优化方案

### 方案1：共享内存 + 智能预加载（推荐）⭐

**核心思路**：
- 使用 `shared_memory` 替代 memmap，数据存储在真实RAM中
- 只存储一份数据，多进程共享访问
- 智能分批加载，避免一次性写入慢的问题

**优势**：
- ✅ 读取速度接近原生内存访问（比memmap快10-50倍）
- ✅ 无需双份存储，直接从Spark写入共享内存
- ✅ 支持增量加载，边加载边训练
- ✅ 多进程DataLoader友好

**实现要点**：
```python
# 核心流程
HDFS -> Spark -> Shared Memory -> Torch Dataset
                     ↑
                (异步预加载)
```

### 方案2：Apache Arrow + Parquet 零拷贝

**核心思路**：
- Spark直接输出Parquet格式（列式存储）
- 使用PyArrow进行零拷贝读取
- 时间步分组存储在Parquet的不同Row Groups中

**优势**：
- ✅ 零拷贝读取，速度快
- ✅ 列式压缩，存储空间小（通常减少50-80%）
- ✅ Spark原生支持，无需额外转换
- ✅ 支持按列过滤，减少I/O

**适用场景**：
- 数据集过大无法全部加载到内存
- 需要长期存储和复用数据

### 方案3：混合内存策略

**核心思路**：
- 热数据（近期访问）保持在内存
- 冷数据使用高效的磁盘缓存（如HDF5/Parquet）
- LRU缓存策略 + 异步预取

**优势**：
- ✅ 平衡内存和速度
- ✅ 适应大规模数据集
- ✅ 自适应优化

## 性能对比（预估）

| 方案 | 读取速度 | 准备时间 | 存储开销 | 适用数据规模 |
|------|---------|---------|---------|------------|
| 当前memmap | 1x (基准) | 很长 | 2x | 大规模 |
| 共享内存 | 10-50x | 短 | 1x | 中等(RAM内) |
| Arrow/Parquet | 5-20x | 最短 | 0.2-0.5x | 大规模 |
| 混合策略 | 5-30x | 中等 | 0.3-1x | 超大规模 |

## 实施建议

### 优先选择：方案1（共享内存）
**如果满足以下条件：**
- 单个训练epoch的数据量 < 可用RAM的60-70%
- 需要最快的训练速度
- 多个实验可能复用同一批数据

### 次优选择：方案2（Arrow/Parquet）
**如果满足以下条件：**
- 数据集过大，无法全部放入内存
- 需要频繁切换不同数据集
- 磁盘I/O性能较好（NVMe SSD）

### 高级选择：方案3（混合策略）
**如果满足以下条件：**
- 数据规模超大（TB级别）
- 内存有限但希望最大化性能
- 访问模式有明显的局部性

## 关键技术细节

### 1. 时间步分组存储优化
保持你的优势设计，但改进实现：
- 每个group是一个连续的内存块
- 滑窗采样时只需计算偏移量，无需复制数据
- 使用view而非copy提高效率

### 2. 多进程DataLoader优化
- 使用 `persistent_workers=True` 避免重复初始化
- 合理设置 `prefetch_factor` 进行异步预取
- 使用 `pin_memory=True` 加速GPU传输

### 3. Spark输出优化
- 控制分区数量，避免小文件问题
- 使用 `repartition/coalesce` 优化输出
- 考虑使用 `cache()` 如果需要多次读取

## 成本收益分析

**短期收益**（1-2周实施）：
- 训练速度提升：2-5倍
- 准备时间减少：60-90%
- 存储空间节省：30-50%

**长期收益**：
- 实验迭代效率提升
- 基础设施成本降低
- 更好的可扩展性
